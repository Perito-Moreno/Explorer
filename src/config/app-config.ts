// Configuration for the application

export const exploreConfig = {
  // Number of properties to load initially
  initialLoadCount: 4,

  // Number of properties to display initially
  initialDisplayCount: 2,

  // Number of additional properties to load when scrolling
  additionalLoadCount: 2,
}
