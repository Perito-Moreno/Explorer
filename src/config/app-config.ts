export const exploreConfig = {
  // Number of properties to load initially from the data source
  initialLoadCount: 10,

  // Number of properties to display initially on the page
  initialDisplayCount: 3,

  // Number of additional properties to load/display when scrolling
  additionalLoadCount: 2,
}
